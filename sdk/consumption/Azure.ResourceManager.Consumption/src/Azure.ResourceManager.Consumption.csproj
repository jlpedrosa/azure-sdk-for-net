<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <Version>1.0.2</Version>
    <PackageId>Azure.ResourceManager.Consumption</PackageId>
    <Description>Azure Resource Manager client SDK for Azure resource provider Azure.ResourceManager.Consumption</Description>
    <PackageTags>azure;management;arm;resource manager;Azure.ResourceManager.Consumption</PackageTags>
    <TemporaryUsePreviousGeneratorVersion>false</TemporaryUsePreviousGeneratorVersion>
    <FileUpgradeFlags>
    </FileUpgradeFlags>
    <UpgradeBackupLocation>
    </UpgradeBackupLocation>
  </PropertyGroup>

  <PropertyGroup>
    <!-- TODO: As part of release process, this PropertyGroup can be removed once RP is approved for namespace -->
    <NoWarn>$(NoWarn);AZC0001</NoWarn>
  </PropertyGroup>
</Project>
